{
  "kata": "s6",
  "description": "Service health: 3-way endpoint() fanout, join, single operations-agent summary.",
  "requiredCalls": {
    "Flow.endpoint": { "min": 3, "max": 3 },
    "Uni.joinAll": { "min": 1, "max": 1 },
    "toAgent": { "min": 1, "max": 1 },
    "ThreadBox.publish": { "min": 1, "max": 1 }
  },
  "forbiddenCalls": ["Flow.agent"],
  "maxAgentCalls": 1,
  "publishCount": { "min": 1, "max": 1 }
}
