/// Placeholder entry point for the `codex exec` design loop. See
/// `README.md` — the `tb-design` row — for scope. Implemented next,
/// after this interim commit, following red/green TDD: the smoke test
/// in `tests/smoke.rs` is written first, against a fake `codex` stub on
/// PATH, before this loop is filled in.
fn main() {
    eprintln!("tb-design: not yet implemented");
    std::process::exit(1);
}
