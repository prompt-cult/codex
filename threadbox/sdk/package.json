{
  "name": "threadbox-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "ThreadBox AssemblyScript SDK skeleton (Phase 1: type-checked kata-authoring surface, no Wasm codegen)",
  "scripts": {
    "check": "asc assembly/threadbox.d.ts --noEmit"
  },
  "dependencies": {
    "assemblyscript": "0.28.20"
  }
}
